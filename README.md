# Content AI — Admin panel

## Setup
```bash
npm install
npm run dev:admin
```

Admin runs on http://127.0.0.1:5174 and proxies `/api` to the backend.

## Production build
Build from repo root with admin Vite config, or serve `admin/dist` after building.
